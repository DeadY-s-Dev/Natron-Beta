org.gradle.jvmargs=-Xmx4G
org.gradle.daemon=true
org.gradle.parallel=true

loom.platform=forge

mod_id=natron
mod_name=Natron
mod_version=1.0.0-beta.1

# Bundles Celeritas (Angelica's renderer) + JOML + fastutil into the jar.
# Off by default: nothing drives Celeritas yet, and it adds ~19 MB (mostly fastutil).
# Turn on to continue the port - CeleritasProbe reports whether it loads on 1.8.9.
includeCeleritas=false
